import 'package:flutter/material.dart';

class CompletedTrades extends StatelessWidget {
  const CompletedTrades({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}