import 'package:flutter/material.dart';

class DisputedTrades extends StatelessWidget {
  const DisputedTrades({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}