import 'package:flutter/material.dart';

class IncompleteTrades extends StatelessWidget {
  const IncompleteTrades({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
     
      
    );
  }
}