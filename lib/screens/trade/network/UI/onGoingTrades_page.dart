import 'package:flutter/material.dart';

class OngoingTrades extends StatelessWidget {
  const OngoingTrades({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}